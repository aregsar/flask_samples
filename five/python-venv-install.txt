# install homebrew
$ ruby -e "$(curl -fsSL https-//raw.github.com/Homebrew/homebrew/go/install)"

# install python 2.7 (installs pip-2.7)
$ brew install python

# install virtualenv
$ pip install virtualenv

# verify install locations
$ which pip
$ which python
$ which virtualenv



